# RMI host
registryHost=localhost

# Port for the RMI registry
# Test port is different to the Standard RMI port.
# This must match the value in <proxy_home>/tests/config/proxy.properties
registryPort=1098

