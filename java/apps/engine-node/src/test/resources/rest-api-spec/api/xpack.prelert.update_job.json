{
  "xpack.prelert.update_job": {
    "methods": [ "PUT" ],
    "url": {
      "path": "/engine/v2/jobs/{job_id}/update",
      "paths": [ "/engine/v2/jobs/{job_id}/update" ],
      "parts": {
        "job_id": {
          "type": "string",
          "required": true,
          "description": "The ID of the job to delete"
        }
      }
    },
    "body": {
      "description" : "A JSON object containing the keys to update and their corresponding new values",
      "required" : true
    }
  }
}
